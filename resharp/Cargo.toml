[package]
name = "nerbench"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
resharp = { version = "0.7.5", features = ["regex_set"] }

[profile.release]
lto = true
codegen-units = 1
